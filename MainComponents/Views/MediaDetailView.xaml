<UserControl x:Class="MediaTracker.Views.MediaDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:MediaTracker.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <helpers:TypeToIconConverter x:Key="TypeIconConv"/>
        <helpers:StatusToBackgroundConverter x:Key="StatusBgConv"/>
        <helpers:StatusToForegroundConverter x:Key="StatusFgConv"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:InverseBooleanConverter x:Key="InvBool"/>

        <!-- Стиль для карточек -->
        <Style x:Key="DetailCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Стиль для лейблов -->
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Padding" Value="0,0,0,5"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F9FAFB">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header Buttons -->
                <RowDefinition Height="Auto"/>
                <!-- Title Card -->
                <RowDefinition Height="*"/>
                <!-- Content Columns -->
            </Grid.RowDefinitions>

            <!-- ROW 0: HEADER BUTTONS -->
            <DockPanel LastChildFill="False" Grid.Row="0" Margin="0,0,0,20">
                <Button DockPanel.Dock="Left" Click="Back_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="←" Margin="0,0,5,0"/>
                        <TextBlock Text="Back"/>
                    </StackPanel>
                </Button>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <!-- View Mode Buttons -->
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}, ConverterParameter=Inverted}">
                        <Button Content="🗑 Delete" Click="Delete_Click" Background="Transparent" Foreground="#EF4444" BorderThickness="1" BorderBrush="#EF4444" Padding="15,6" Margin="0,0,10,0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Content="Edit" Click="Edit_Click" Background="White" BorderThickness="1" BorderBrush="#D1D5DB" Padding="15,6">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>

                    <!-- Edit Mode Buttons -->
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                        <Button Content="Cancel" Click="Cancel_Click" Background="White" BorderThickness="1" BorderBrush="#D1D5DB" Padding="15,6" Margin="0,0,10,0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Content="Save" Click="Save_Click" Background="#111827" Foreground="White" Padding="15,6">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </DockPanel>

            <!-- ROW 1: TITLE CARD -->
            <Border Grid.Row="1" Style="{StaticResource DetailCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icon -->
                    <Border Grid.Column="0" Width="80" Height="80" Background="#F3F4F6" CornerRadius="12" Margin="0,0,20,0">
                        <TextBlock Text="{Binding TempItem.Type, Converter={StaticResource TypeIconConv}}" 
                                   FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <!-- Info -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TempItem.Title}" FontSize="24" FontWeight="Bold" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding TempItem.OriginalTitle}" Foreground="Gray" Margin="0,2,0,10" 
                                   Visibility="{Binding TempItem.OriginalTitle, Converter={StaticResource StringToVisConv}}"/>

                        <!-- Badges -->
                        <WrapPanel Orientation="Horizontal">
                            <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,5,5">
                                <TextBlock Text="{Binding TempItem.Year, StringFormat='📅 {0}'}" FontSize="12"/>
                            </Border>
                            <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,5,5" Visibility="{Binding TempItem.Country, Converter={StaticResource StringToVisConv}}">
                                <TextBlock Text="{Binding TempItem.Country, StringFormat='🌍 {0}'}" FontSize="12"/>
                            </Border>
                            <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,5,5" Visibility="{Binding TempItem.DurationMinutes, Converter={StaticResource ShowIfNotEmpty}}">
                                <TextBlock Text="{Binding TempItem.DurationMinutes, StringFormat='⏱ {0} min'}" FontSize="12"/>
                            </Border>
                            <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,5,5" Visibility="{Binding TempItem.OfficialRating, Converter={StaticResource StringToVisConv}}">
                                <TextBlock Text="{Binding TempItem.OfficialRating, StringFormat='🔞 {0}'}" FontSize="12"/>
                            </Border>
                        </WrapPanel>

                        <!-- Tags -->
                        <TextBlock Text="{Binding TempItem.TagsString}" Foreground="#2563EB" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ROW 2: COLUMNS -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <!-- Left: Info -->
                    <ColumnDefinition Width="20"/>
                    <!-- Spacer -->
                    <ColumnDefinition Width="2*"/>
                    <!-- Right: Tabs -->
                </Grid.ColumnDefinitions>

                <!-- LEFT COLUMN -->
                <StackPanel Grid.Column="0">
                    <!-- Synopsis -->
                    <Border Style="{StaticResource DetailCard}">
                        <StackPanel>
                            <TextBlock Text="Description" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding TempItem.Synopsis}" TextWrapping="Wrap" Foreground="#4B5563"/>
                        </StackPanel>
                    </Border>

                    <!-- Characteristics -->
                    <Border Style="{StaticResource DetailCard}">
                        <StackPanel>
                            <TextBlock Text="Characteristics" FontWeight="Bold" Margin="0,0,0,10"/>

                            <Grid Margin="0,0,0,5">
                                <TextBlock Text="Type" Foreground="Gray"/>
                                <TextBlock Text="{Binding TempItem.Type}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,5" Visibility="{Binding TempItem.SeasonsCount, Converter={StaticResource ShowIfNotEmpty}}">
                                <TextBlock Text="Seasons" Foreground="Gray"/>
                                <TextBlock Text="{Binding TempItem.SeasonsCount}" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,5" Visibility="{Binding TempItem.EpisodesCount, Converter={StaticResource ShowIfNotEmpty}}">
                                <TextBlock Text="Episodes" Foreground="Gray"/>
                                <TextBlock Text="{Binding TempItem.EpisodesCount}" HorizontalAlignment="Right"/>
                            </Grid>

                            <Separator Margin="0,10"/>
                            <TextBlock Text="{Binding TempItem.AddedDate, StringFormat='Added: {0}'}" Foreground="Gray" FontSize="11"/>
                            <TextBlock Text="{Binding TempItem.UpdatedDate, StringFormat='Updated: {0}'}" Foreground="Gray" FontSize="11"/>
                        </StackPanel>
                    </Border>

                    <!-- Related Items -->
                    <Border Style="{StaticResource DetailCard}" Visibility="{Binding RelatedItems.Count, Converter={StaticResource ShowIfNotEmpty}}">
                        <StackPanel>
                            <TextBlock Text="Related Media" FontWeight="Bold" Margin="0,0,0,10"/>
                            <ItemsControl ItemsSource="{Binding RelatedItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F9FAFB" CornerRadius="6" Padding="8" Margin="0,0,0,8" BorderBrush="#E5E7EB" BorderThickness="1">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Type}" FontSize="10" Foreground="Gray"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- RIGHT COLUMN -->
                <TabControl Grid.Column="2" BorderThickness="0" Background="Transparent">
                    <TabItem Header="My Progress" FontSize="14">
                        <Border Style="{StaticResource DetailCard}" Margin="0,10,0,0">
                            <StackPanel>
                                <Label Content="Status"/>
                                <ComboBox x:Name="StatusCombo" IsEnabled="{Binding IsEditing}" Margin="0,0,0,15" Padding="6" SelectedIndex="0">
                                    <ComboBoxItem Content="Planned"/>
                                    <ComboBoxItem Content="Watching"/>
                                    <ComboBoxItem Content="Reading"/>
                                    <ComboBoxItem Content="Completed"/>
                                    <ComboBoxItem Content="Paused"/>
                                    <ComboBoxItem Content="Abandoned"/>
                                </ComboBox>

                                <Label Content="Rating (1-10)"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBox Text="{Binding TempItem.Rating}" IsEnabled="{Binding IsEditing}" Width="60" Padding="5"/>
                                    <TextBlock Text="/ 10" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </StackPanel>

                                <Label Content="Personal Notes"/>
                                <TextBox Text="{Binding TempItem.Notes}" IsEnabled="{Binding IsEditing}" 
                                         Height="100" TextWrapping="Wrap" AcceptsReturn="True" Padding="5" 
                                         BorderBrush="#E5E7EB" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </TabItem>

                    <TabItem Header="Inventory" FontSize="14">
                        <Border Style="{StaticResource DetailCard}" Margin="0,10,0,0">
                            <StackPanel>
                                <!-- Toggles -->
                                <Grid Margin="0,0,0,10">
                                    <StackPanel>
                                        <TextBlock Text="Physical Copy" FontWeight="Bold"/>
                                        <TextBlock Text="I own a physical copy" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                    <CheckBox IsChecked="{Binding TempItem.HasPhysicalCopy}" IsEnabled="{Binding IsEditing}" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                </Grid>

                                <Grid Margin="0,0,0,20">
                                    <StackPanel>
                                        <TextBlock Text="Digital Copy" FontWeight="Bold"/>
                                        <TextBlock Text="I own a digital copy" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                    <CheckBox IsChecked="{Binding TempItem.HasDigitalCopy}" IsEnabled="{Binding IsEditing}" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                </Grid>

                                <Separator Margin="0,0,0,15"/>

                                <!-- Inventory Fields -->
                                <Label Content="Format"/>
                                <TextBox Text="{Binding TempItem.Format}" IsEnabled="{Binding IsEditing}" Padding="5" Margin="0,0,0,15"/>

                                <Label Content="Source"/>
                                <TextBox Text="{Binding TempItem.Source}" IsEnabled="{Binding IsEditing}" Padding="5" Margin="0,0,0,15"/>

                                <Label Content="Location"/>
                                <TextBox Text="{Binding TempItem.Location}" IsEnabled="{Binding IsEditing}" Padding="5" Margin="0,0,0,15"/>

                                <Label Content="Price"/>
                                <TextBox Text="{Binding TempItem.PurchasePrice}" IsEnabled="{Binding IsEditing}" Padding="5"/>
                            </StackPanel>
                        </Border>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>