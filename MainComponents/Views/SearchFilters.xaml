<UserControl x:Class="MainComponents.Views.SearchFilters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Стиль для Label -->
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Foreground" Value="#374151"/>
        </Style>

        <!-- Стиль для ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="2" CornerRadius="12" Padding="16">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <TextBlock Text="Search &amp; Filters" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>

                <!-- Clear Button (Visible only when filters active) -->
                <Button HorizontalAlignment="Right" Command="{Binding ClearCommand}"
                        Visibility="{Binding HasActiveFilters, Converter={StaticResource BoolToVis}}"
                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✕" Foreground="#EF4444" Margin="0,0,5,0"/>
                        <TextBlock Text="Clear" Foreground="#EF4444"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- Filters Grid -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 1. Search -->
                <StackPanel Grid.Column="0">
                    <Label Content="Search"/>
                    <Grid>
                        <!-- Иконка лупы -->
                        <TextBlock Text="🔍" Margin="8,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Panel.ZIndex="1" Opacity="0.5" IsHitTestVisible="False"/>
                        <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                                 Padding="28,0,5,0" Height="35" VerticalContentAlignment="Center" BorderBrush="#E5E7EB" BorderThickness="1">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </Grid>
                </StackPanel>

                <!-- 2. Type -->
                <StackPanel Grid.Column="2">
                    <Label Content="Type"/>
                    <ComboBox ItemsSource="{Binding Types}" SelectedItem="{Binding SelectedType}">
                    </ComboBox>
                </StackPanel>

                <!-- 3. Year -->
                <StackPanel Grid.Column="4">
                    <Label Content="Year"/>
                    <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}">
                    </ComboBox>
                </StackPanel>

                <!-- 4. Tag -->
                <StackPanel Grid.Column="6">
                    <Label Content="Tag"/>
                    <ComboBox ItemsSource="{Binding AvailableTags}" SelectedItem="{Binding SelectedTag}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>