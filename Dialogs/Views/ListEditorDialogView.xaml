<UserControl x:Class="MediaTracker.Views.ListEditorDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="350" Width="400"
             Background="White">
    <StackPanel Margin="20">
		<TextBlock Text="Manage List" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

		<Label Content="List Name" FontWeight="SemiBold"/>
        <TextBox Text="{Binding List.Name, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,10"/>

		<Label Content="Description" FontWeight="SemiBold"/>
        <TextBox Text="{Binding List.Description, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="5" Margin="0,0,0,10"/>

		<Label Content="List Type" FontWeight="SemiBold"/>
        <ComboBox Padding="5" Margin="0,0,0,20" SelectedIndex="0" ItemsSource="{Binding AvailableTypes}" SelectedItem="{Binding List.Type, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Padding="15,6" Margin="0,0,10,0" Command="{Binding CancelCommand}"/>
            <Button Content="Save" IsDefault="True" Padding="15,6" Background="Black" Foreground="White" Command="{Binding SaveCommand}"/>
		</StackPanel>
	</StackPanel>
</UserControl>